services:
  etl-analytics:
    build: .
    env_file: .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    networks:
      - default
      - proxy
    restart: unless-stopped

networks:
  proxy:
    external: true
